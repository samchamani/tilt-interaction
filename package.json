{
  "name": "tilt-interaction",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "vite --host",
    "start:dev": "vite build && rm -rf public/* && mv -f dist/* public/ && rm -rf dist && http-server --ssl --cert cert.pem --key key.pem",
    "build" : "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
